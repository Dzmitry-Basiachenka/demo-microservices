spring:
  datasource:
    url: jdbc:tc:postgresql:15.0:///resource-service-db
  cloud:
    discovery:
      enabled: false

com:
  microservices:
    service:
      api-gateway:
        url: http://localhost:8099
